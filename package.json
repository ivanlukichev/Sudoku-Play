{
  "name": "sudokus-org",
  "version": "1.0.0",
  "private": true,
  "description": "Sudoku Play website and browser extension source.",
  "type": "module",
  "scripts": {
    "build:extensions": "node scripts/build-extensions.mjs",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4"
  }
}
